type item called testItem1
    method use
        .name = "Testing Item #1"
        print $"Player used this item: {This.name}"
        interop start
            for (var i = 0; i < 3; i++)
            {
                Console.WriteLine("Hello from interop!");
            } 
        end
    end
end

type item called testItem2
    method use
        .name = "hello"
    end
end

type item called testItem3
    method use
        .i++
    end
end